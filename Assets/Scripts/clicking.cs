using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class clicking : MonoBehaviour
{
    // Start is called before the first frame update
    public int index;
    public controll indexss;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void OnMouseDown() {
        
    }

}
